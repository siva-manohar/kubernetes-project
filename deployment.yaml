---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: ""
  namespace: "default"
  labels:
    app: "k8s-node"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "k8s-node"
  template:
    metadata:
      labels:
        app: "k8s-node"
    spec:
      containers:
        - name: ""
          image: "public.ecr.aws/e4y1d5a4/helloworld"
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"

---
apiVersion: "autoscaling/v2beta1"
kind: "HorizontalPodAutoscaler"
metadata:
  name: ""
  namespace: "default"
  labels:
    app: "k8s-node"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: ""
    apiVersion: "apps/v1beta1"
  minReplicas: 1
  maxReplicas: 1
  metrics:
    - type: "Resource"
      resource:
        name: "cpu"
        targetAverageUtilization: 80
