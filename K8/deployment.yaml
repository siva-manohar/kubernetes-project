apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "project1"
  namespace: "backend"
  labels:
    app: "k8s-node"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "k8s-node"
  template:
    metadata:
      labels:
        app: "k8s-node"
    spec:
      containers:
        - name: "project1"
          image: "599032366287.dkr.ecr.ap-south-1.amazonaws.com/project1:SAMPLE-PROJECT-buildNumber"
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
